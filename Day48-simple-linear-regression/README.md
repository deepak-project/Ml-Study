# Creating-SLR c

We know the simple linear regression machine learning model just a best fit straight line of our data
y = m * x + b

if we we can find the value of m and b then we can predict the value of our input data
 
simple linear regression model when we train then hi calculate the value of m and b

in mathmatically the formula of m and b is define you can search on web 

using of class i make a algorithim where i implement the formula of m and b in fit function

and create the line of using value m and b in predict function

now when i call the function fit my algorithim just find the value of m anb b 

then just take the value of input data and call the predict function hi predict the value of using the straight line
